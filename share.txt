function Send-ApplicationInsightsLogWithManagedIdentity {
    param (
        [string]$AppInsightsResourceId,
        [string]$LogMessage
    )

    $accessToken = Get-AzAccessToken -ResourceUrl "https://api.applicationinsights.io" -ErrorAction SilentlyContinue

    if (-not $accessToken) {
        Write-Host "Błąd: Nie można uzyskać tokena uwierzytelniającego dla Identity zarządzanego przez środowisko wykonywania."
        return
    }

    $uri = "https://api.applicationinsights.io/v1/apps/$AppInsightsResourceId/logs"

    $headers = @{
        "Authorization" = "Bearer $($accessToken.Token)"
    }

    $body = @{
        "message" = $LogMessage
    }

    try {
        $response = Invoke-RestMethod -Uri $uri -Headers $headers -Method 'POST' -Body (ConvertTo-Json $body)
        Write-Host "Testowy log został wysłany do Application Insights."
    }
    catch {
        Write-Host "Wystąpił błąd podczas wysyłania logu: $_"
    }
}



$appInsightsResourceId = "/subscriptions/{subID}/resourceGroups/{resourceGroupName}/providers/microsoft.insights/components/{appInsightsName}"
$logMessage = "To jest testowy log wysłany do Application Insights za pomocą Identity zarządzanego przez środowisko wykonywania."

Send-ApplicationInsightsLogWithManagedIdentity -AppInsightsResourceId $appInsightsResourceId -LogMessage $logMessage
